{
    "installCommand": "npm install",
    "buildCommand": "npm run build",
    "version": 2,
    "builds": [
      { "src": "index.js", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/" }
    ]
  }